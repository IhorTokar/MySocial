spring:
  application:
    name: social-server

  datasource:
    url: jdbc:postgresql://localhost:5432/social_network
    username: social_app
    password: frogchairuwu9AB
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate     # схему створює ТІЛЬКИ Flyway, Hibernate лише перевіряє
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:w4mw7pX1bVdP4LdkIqJTO8heH1OALGIM8OwERQcW7n0=}
  expiration-ms: 86400000

neo4j:
  uri: bolt://localhost:7687
  username: neo4j
  password: neo4jpassword123

app:
  upload:
    dir: uploads